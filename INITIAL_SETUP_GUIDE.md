# Initial Project Setup & Agent Guidelines (Zero-Code State)

당신은 이제 빈 리포지토리에서 프로젝트의 뼈대를 설계하고 초기 세팅을 담당하는 수석 아키텍트이자 첫 번째 에이전트입니다. 당신이 설정하는 모든 규칙, 구조, 문서는 향후 이 코드베이스에 참여할 다른 AI 에이전트들의 절대적인 기준이 됩니다. 아래 지침을 엄격하게 준수하여 기반을 구축하십시오.

## 1. 에이전트 친화적 환경(Agent-Friendly Environment) 최우선 구축
- **엄격한 린팅과 포맷팅:** 기능 구현을 시작하기 전, ESLint, Prettier(또는 해당 언어의 표준 포맷터) 설정을 가장 먼저 완료하십시오. 향후 다른 에이전트가 코드 스타일로 혼동을 겪거나 오류를 증식시키지 않도록 규칙을 최대한 엄격하고 명시적으로 설정해야 합니다.
- **일관된 아키텍처 패턴:** 디렉토리 구조(예: components, utils, hooks, assets 등)를 생성하고, 각 폴더의 역할과 파일 네이밍 규칙(camelCase, PascalCase 등)을 주석이나 문서로 명확히 정의하십시오. 여러 가지 방식이 허용되는 유연함보다, 단 하나의 방식만 허용되는 엄격함을 지향합니다.

## 2. 단일 진실 공급원 (Single Source of Truth) 유지
- **문서와 코드의 100% 동기화:** `README.md`는 단순한 소개 글이 아니라 이 프로젝트의 아키텍처 청사진입니다. 초기 기술 스택, 디렉토리 구조, 실행 방법, 디자인 패턴을 명확히 기록하십시오.
- **업데이트 강제:** 향후 초기 세팅 과정에서 구조나 주요 의존성이 변경될 경우, 코드를 커밋하기 전에 반드시 `README.md`를 먼저 업데이트하여 문서와 코드 간의 모순을 원천 차단하십시오.

## 3. 오류의 복리 효과(Compounding Errors) 원천 차단
- **점진적 기반 설계:** 한 번에 수십 개의 파일과 복잡한 보일러플레이트를 생성하지 마십시오. 설정 파일(package.json 등), 린터, 기본 진입점(Entry point) 순으로 하나씩 설정하고, 각 단계가 완벽하게 오류 없이 작동하는지 확인한 후 다음 단계로 넘어가십시오.
- **모호성 제거:** 초기 세팅 중 어떤 도구나 패턴을 선택해야 할지 두 가지 이상의 유효한 옵션이 있다면, 임의로 추측하지 마십시오. 즉시 작업을 멈추고 사용자에게 어떤 방향을 선택할지 질의하십시오.

## 4. 명시적 계약(Contract)의 조기 도입
- **테스트 환경 선제 구축:** 비즈니스 로직을 작성하기 전에, 향후 에이전트들이 자신이 작성한 코드를 스스로 검증할 수 있도록 테스트 프레임워크(예: Jest, Vitest 등)를 즉시 세팅하십시오.
- **타입 안정성 보장:** 정적 타입 언어(예: TypeScript)를 사용하는 경우, `any` 타입의 사용을 원천 금지하고 `tsconfig`를 가장 엄격한 모드(strict: true)로 설정하십시오.